import { AuthCard } from "@/components/auth/auth-card";

export default function CreateAccountPage() {
  return <AuthCard title="Create your account" subtitle="Start with your profile, then create your first business." mode="create-account" />;
}

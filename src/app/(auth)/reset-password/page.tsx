import { AuthCard } from "@/components/auth/auth-card";

export default function ResetPasswordPage() {
  return <AuthCard title="Choose a new password" subtitle="Use a strong password that is not shared with another service." mode="reset" />;
}
